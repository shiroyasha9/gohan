{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "extends": ["ultracite/biome/core"],
  "files": {
    // reference datasets, shipped plugin bundles, and readme assets are generated, not linted
    "includes": ["!data/reference", "!plugin/scripts", "!assets"]
  },
  "overrides": [
    {
      // package entry points are barrel files by design
      "includes": ["packages/*/src/index.ts"],
      "linter": {
        "rules": {
          "performance": {
            "noBarrelFile": "off"
          }
        }
      }
    }
  ]
}
